# CLAUDE.md

Read @AGENTS.md first. It holds the shared project guidance for every coding agent.

## Claude-specific

- Git commits: **do not** add a `Co-Authored-By: Claude` trailer (or any Claude attribution) to commit messages or PR descriptions. This is the maintainer's explicit preference.
- Don't push unless asked.
- Run `pytest` before committing.
